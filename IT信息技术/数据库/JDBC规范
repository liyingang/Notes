使用：
	1.导入jar包驱动
		Class.forname("dirver");
	2.获取对象
		Connection conn= DriverManager.getConnection("jdbc:Name://host:port","username","password")
	3.获取statement
		Statement stmt=con.createStatement();

	4.执行sql
		stmt.executeUpdate("sql satement");

	5.输出结果
	6.释放资源
		stmt.close();
		conn.close();

对象：
	DriverManager：类
		驱动管理对象
